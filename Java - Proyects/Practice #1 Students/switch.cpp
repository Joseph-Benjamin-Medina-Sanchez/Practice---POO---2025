#include<iostream>
#include<string.h>

using namespace std;

int main (){
    int contador;

    do{
        cout<<"Cuantos numeros desea guardar?"<<endl;
        cin>>contador;
    } while (contador < 0);
    
    
}